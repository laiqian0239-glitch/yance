# 言策 Batch 19｜全平台平面文档流布局决策

## 1. 决策结论

言策所有生产页面的主要功能模块必须属于同一个页面文档流，不得继续使用“固定高度底部工作台、固定视口卡片、多层内部滚动”承载主要业务内容。

允许左右分栏，但左右栏必须由内容自然撑高，并共同参与页面级滚动；分栏本身不得形成两个只能看到局部内容的小窗口。

## 2. 唯一主滚动权威

生产页面统一采用：

```text
Windows Electron 应用壳
└─ 当前生产页面唯一主滚动
   ├─ 页面标题与状态
   ├─ 汇总与统计
   ├─ 目录/列表模块
   ├─ 当前对象详情
   ├─ 操作、证据、诊断及后续模块
   └─ 页面底部
```

主要模块必须满足：

- `height:auto`；
- `max-height:none`；
- `overflow:visible`；
- 内容自然撑开；
- 页面滚动时连续浏览；
- 不以“内部滚动后可达”代替“完整展开”。

## 3. 允许保留内部滚动的例外

仅以下真正需要独立视口的组件允许内部滚动：

- 会话消息历史；
- 弹窗与抽屉；
- 下拉菜单和选择列表；
- 明确标识的技术日志/代码查看器；
- 经产品与性能评审确认的超长虚拟化数据表。

联系人、客户档案、关系轨迹、关系洞察、AI 工作台、账号与平台、系统中心、设置与恢复、主题工作室的主要内容不属于例外。

## 4. Batch 19 实现

新增最后加载的公共布局权威：

`frontend/r32-flat-document-flow.css`

它统一覆盖此前互相冲突的 Fix15、Batch 14、Batch 16 和 Batch 18 布局规则：

- 应用壳成为当前生产页面唯一纵向滚动根；
- 主导航保持可用，但不截断页面；
- 九个生产工作区恢复自然高度；
- 目录与详情可在桌面端左右分栏，但不再各自形成小滚动窗口；
- 低宽度窗口自动改为纵向展开；
- AI 工作台原先被隐藏或压缩的核心、标签、最近操作和撤销区恢复到连续页面；
- 系统和设置导航与正文处于同一页面流；
- 主题正文不再被固定高度视口截断。

## 5. 验收口径

源码或 Chromium 检查只能作为预检查。最终必须在真实 Windows Electron 中证明：

- 当前页面只有一根主要纵向滚动条；
- 主要模块没有独立纵向滚动条；
- 模块内容不被固定容器裁切；
- 页面从顶部到页面底部可连续浏览；
- 100%、125%、150% 缩放均满足；
- 真实字体、真实数据量和动态加载后仍满足。

真实 Windows 证据完成前，布局缺陷不得关闭。
