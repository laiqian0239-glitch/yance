# 言策 f25fe2e 真实 Windows UAT 修复进展 12

## 1. 结论

本批次完成了 `WP-F25-08` 中桌面路由错位与标题/页签/正文重叠的第一组共同根因修复，但**没有关闭 50 项去重问题，也没有解除 Windows UAT 阻断**。

当前治理状态保持：

- `repairState=REPAIR_ATTEMPT_IN_PROGRESS`
- `windowsUatState=WINDOWS_UAT_BLOCKED`
- `formalRelease=false`
- `readyForPromotion=false`
- 原始权威基线：`f25fe2e2b4f065d2c09de034eddb67857eeb83bb / 015f7969a2363952071bf11f4da3eb2adaf7edbf`
- 本批修复基底：`1f0a72b321e5ddc4d9404654e024575bea877bf3 / 9e633b3b754953523554343ac7cda804060acc95`

## 2. 已确认根因

### 根因 A：页面路由权威分散

联系人、档案、轨迹、洞察、AI 工作台、账号中心、系统中心、设置与恢复、主题与外观分别增删自己的 `.app` 路由 class。异常、托盘恢复或跨模块跳转时，旧路由 class 可能残留，导致菜单高亮、标题和主内容不一致。

### 根因 B：长页滚动补丁覆盖原始 Grid

后加载的 `r32-workspace-scroll-layout.css` 曾把多个生产页面改成 `display:block` 和外层自然滚动，同时产品页签又动态插入标题与正文之间，造成同一页面存在多个相互竞争的布局权威。

### 根因 C：主题页审计区插入到错误父节点

`r32-theme-computed-audit.js` 以主题工作区为父节点，却使用位于 `.theme32-body` 内的 footer 作为 `insertBefore` 参考节点，浏览器会抛出 DOM `NotFoundError`，并可能中断后续路由/页签挂载。

## 3. 本批代码修复

1. 扩展 `r32-workspace-route-authority.js`：
   - 原子清除全部生产路由 class；
   - 每次只保留一个路由；
   - 清除沉浸、联系人隐藏、AI 覆盖层等跨页面残留布局状态；
   - 记录目标页面、实际页面、来源和完整性；
   - MutationObserver 在旧模块误加第二个路由时自动恢复权威目标。
2. 将联系人、档案、轨迹、会话、洞察、AI 工作台、账号中心、系统中心、设置与恢复、主题与外观全部接入统一路由事务。
3. 修正主题审计区 DOM 插入：只在参考节点与容器同父时使用 `insertBefore`，否则追加到 `.theme32-body`。
4. 修正产品页签挂载：只使用工作区直接子节点作为参考，避免跨父节点插入异常。
5. 在布局文件末尾建立生产页面单一 Grid 权威：标题、产品页签、正文各占独立行，正文拥有唯一安全滚动区；窄窗口改为单列自然流。
6. 新增行为回归测试，验证路由切换是原子的，并能修复旧模块制造的双路由冲突。

## 4. 非字符串运行预检

本批使用实际 Chromium 页面运行和 DOM 点击，不以字符串/正则结果代替 UI：

- 9 个生产页面；
- 窗口：1440×900、1100×760、760×760；
- 缩放模拟：100%、125%、150%；
- 页面布局矩阵：81/81 通过；
- 用户可见主导航→产品页签→生产页面点击矩阵：33/33 通过；
- 浏览器 page error：0；
- 检查内容：单一路由、唯一可见生产工作区、标题/页签/正文无几何重叠、工作区不横向溢出、正文滚动区可达。

证据位于 `artifacts/repair-batch12/chromium-preflight/`。

## 5. 自动回归

- 修改 JavaScript `node --check`：通过；
- Batch 12 路由行为测试：2/2 通过；
- 相关 UAT 回归：29/29 通过；
- Chromium 布局矩阵：81/81 通过；
- Chromium 生产点击矩阵：33/33 通过。

## 6. 未关闭项

- `F25-D44`：代码根因已修，仍需真实 Windows 故障、切页、隐藏/显示、托盘恢复后验证菜单/路由/标题/内容一致。
- `F25-D46`：代码根因已修，仍需真实 Windows 100%/125%/150% 缩放、宽窄窗口、滚动和托盘恢复截图。
- `F25-D45`：托盘恢复和页面切换 P95 性能本批未证明。
- `F25-D47～D50`：本批没有宣告关闭。
- `F25-D01～D43` 及 `F25-G01～G09`：均未因本批前端修复而关闭。

## 7. 下一步真实 Windows 验收路径

1. 启动本批源码，确认运行身份与本批交付回执一致。
2. 依次打开：会话中心、联系人与关系四个页签、AI 回复大脑、账号与平台、系统与设置三个页签。
3. 每页执行 100%/125%/150% 缩放和宽/窄窗口检查。
4. 在系统与设置页面执行：切页→最小化托盘→显示主窗口→继续切页→滚动到底→返回顶部。
5. 保存每一步截图、窗口尺寸、缩放、路由 class、激活导航、页面标题、首个可见正文节点、耗时和运行日志。
6. 只有真实 Windows 证据通过后，才能把 `F25-D44/F25-D46` 从 `CODE_REPAIRED_WINDOWS_PENDING` 改为 `CLOSED`。
