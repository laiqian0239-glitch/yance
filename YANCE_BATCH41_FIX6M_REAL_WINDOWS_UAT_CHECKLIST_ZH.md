# Yance Batch41 FIX6M 真实 Windows 三平台与 AI 闭环 UAT 清单

## 1. 前置规则

- 使用 FIX6M 一键启动包的隔离数据入口；
- 完全退出旧版言策及其后台进程；
- 分别准备真实 WhatsApp、Telegram、Facebook 公共主页测试账号；
- 只向明确授权的测试联系人发送内容；
- 本轮未执行 OpenRouter 正式专项评估，不得把候选模型标记为正式冠军；
- 每项必须保存截图、脱敏诊断、traceId/executionId、平台消息 ID 和时间戳。

## 2. 每个平台必须独立完成的矩阵

对 WhatsApp、Telegram、Facebook 公共主页分别执行：

1. **登录**：首次认证成功；错误凭据有明确原因码；取消可终止；等待状态可追踪。
2. **重启恢复**：关闭应用后重启，会话恢复或明确要求重新认证，不得永久转圈。
3. **联系人同步**：联系人数量、名称、平台外部 ID 与平台一致；同名联系人不得误合并。
4. **联系人头像**：头像显示、缓存、更新、失效重取均有证据；失败显示原因而非空白。
5. **历史聊天记录**：分页回填、断点续传、重复去重、乱序收敛；重启后不丢失。
6. **文本收发**：入站和出站内容完整显示，Unicode、德语字符和长文本不为空。
7. **图片/文件/语音**：下载、缩略图、播放/打开、发送和失败重试状态正确。
8. **GIF**：入站和出站可播放；无法原生发送时显示明确降级方式。
9. **动态贴纸**：Telegram 原生贴纸、WhatsApp 贴纸、Facebook 附件分别验证；不得统一误当静态图片或空消息。
10. **引用/回复**：引用目标、发送方和正文投影正确。
11. **平台回执**：accepted、delivered、read、failed 单调收敛；晚到事件不得回退成功状态。
12. **断网与恢复**：断网、恢复、平台限流和凭据过期均进入可重试或需人工处理状态。
13. **退出/撤销**：断开账号后停止事件和发送，其他平台不受影响。

## 3. 持久任务与延迟等待

- 登录、历史同步、头像/媒体下载、消息发送和回执核对均可看到 executionId；
- RUNNING/WAITING_REMOTE 有心跳和最后活动时间；
- 取消请求在重启后仍存在；
- retry 显示下次执行时间和错误分类；
- 应用崩溃后任务可恢复，不重复发送；
- 陈旧 worker 结果被 generation fencing 拒绝；
- 无任务可以无限显示“正在等待”而没有原因码。

## 4. 联系人与关系界面

- 同一真实人物在不同平台先显示为独立 ExternalIdentity；
- 只有人工确认后才能绑定到同一 ContactAggregate；
- 联系人详情、人物档案、关系轨迹、会话中心和 AI 工作台使用同一 contactId；
- 关系断言可以展开到真实消息证据；
- 拒绝或撤销关系断言后生成新上下文快照，历史证据仍保留；
- 删除/断开平台账号不应删除联系人历史与审计链。

## 5. AI 回复闭环

- 新入站消息持久化后，AI 回复候选读取正确 contact 快照；
- 候选模式必须 `humanReviewRequired=true`；
- 未确认候选不得发送；
- 人工修改后发送，traceId 贯穿路由、模型、人工确认、平台发送和回执；
- providerRequestId 与平台 delivery attempt/receipt 均可在脱敏诊断中关联；
- 发送失败不得显示为成功，也不得进入 AI 学习。

## 6. AI 学习闭环

- 入站消息、未审核候选、紧急模式、发送失败均不得创建可激活学习；
- 人工审核且平台成功发送后只创建 pending 学习收据；
- approve → shadow → active 每一步有 actor、时间和原因；
- 只有 active 版本可被 AI 检索；
- 每次检索产生 retrieval receipt 并关联 traceId；
- revoke/rollback 后新回复不再使用该版本；
- 学习证据导出不得包含 API Key、凭据或未脱敏完整聊天正文。

## 7. 影子模式与切换门禁

- CommunicationAuthority、ContactRelationshipAuthority、AIReplyLearningAuthority 分别累积影子比较样本；
- 样本不足时必须阻断切换；
- 任意 mismatch 必须显示 fail 并可定位 scopeId；
- 验收窗口内全部样本一致后，只能提出切换申请，不能由 UI 自动切换；
- 切换前后均保留可回滚路径和历史收据。

## 8. 关闭条件

只有以下全部满足才可关闭 FIX6M：

- 三个平台真实登录、恢复、联系人头像、历史聊天记录和文本收发通过；
- GIF、动态贴纸、图片、语音和文件有成功或明确失败证据；
- 所有延迟等待均由持久任务解释并可取消/恢复；
- 平台回执单调收敛且无重复发送；
- 联系人与关系界面使用统一身份和真实消息证据；
- AI 回复从入站到人工确认、发送和回执使用同一 trace；
- AI 学习从 pending 到 active、检索、撤销和回滚完整通过；
- 影子验收窗口样本充足且 mismatch=0；
- 真实 Windows 证据包校验通过。

在上述真实 Windows UAT 未执行前，必须保持 `readyForPromotion=false` 和 `formalRelease=false`。
