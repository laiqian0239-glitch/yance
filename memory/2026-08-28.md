# 言策发布上线收尾 — V21 Boot Critical Path Durable Handoff

> Durable project memory / execution handoff. This file is deliberately kept on a separate `memory/*` branch so it does not disturb the active Windows worktree or checkpoint branch. It is not a production candidate and must not be merged by itself.

## Hard execution rules

- Fast Landing = shortest causal path → aggregate same-root fixes → one new exact head → one necessary CI validation → ordinary merge immediately if eligible → next Windows RC/UAT.
- Same exact head must never rerun the same CI. Existing workflow/job logs/status are evidence; do not rerun for reassurance.
- No squash, rebase, amend, force-push, cherry-pick, or history rewrite.
- Frozen historical PRs are evidence only. Release/publish/ledger/final UAT remain fail-closed.
- UAT/RC large files default to Motrix using a fresh GitHub signed URL.
- Do not claim final root-cause closure until the full marker→fork graph is enumerated, focused RED→GREEN is complete, the regression guard is clean, and a new same-build Windows startup capsule passes.

## Repository / worktree

- Repo: `laiqian0239-glitch/yance`
- Active Windows worktree: `C:\GitHub\yance-boot-critical-async-durability`
- Alternate dependencies if needed: `C:\GitHub\yance-electron-lifecycle\node_modules`
- Set `NODE_PATH=C:\GitHub\yance-electron-lifecycle\node_modules` when the active worktree has no `node_modules`.
- Baseline parent for this causal batch: `d5a5e8ff1f41352a2f400ed37138ea14ef820484` (#909 ordinary merge).

## Remote safety checkpoints

1. `93b9df438c25c9eff1b8a414e3425467600ca714`
   - branch: `checkpoint/v21-boot-critical-async-20260828`
   - first safety checkpoint

2. `810799ba1eb7b45cf9d4b47b7b717754c1027b5a`
   - same checkpoint branch, ordinary fast-forward from `93b9df43`
   - commit: `checkpoint(v21): preserve boot-critical async propagation round 2 [skip ci]`
   - 30 tracked files saved, unrelated files = 0
   - this is a REMOTE SAFETY CHECKPOINT only, NOT a freeze point and NOT a production candidate

Never reset/restore the active worktree back to `810799ba`. Continue incrementally from the current local worktree. The checkpoint only protects already-completed work from loss.

Six local `diag-*.js` scripts (`diag-genesis`, `diag-prepare`, `diag-prepare2`, `diag-timing`, `diag-write-error`, `diag-write-error2`) were intentionally left untracked and are not deliverables. Do not commit them merely to make the tree clean.

## Completed and reusable work at checkpoint `810799ba`

These changes are valuable and must be preserved unless a later concrete failure proves one of them wrong:

- `BackendProcessHost` trusted Node runtime probe: sync process probing replaced by bounded async `execFile` path.
- Boot-reachable launch validation asyncified:
  - `pathExists` uses async access
  - `firstExistingDelimitedPath` uses `for...of` + `await` (never `Array.find(async ...)`)
  - `validateReleaseStartupConfig` async
  - `validateBackendLaunchContract` async
  - `_startUnlocked` awaits validation
- `backend/tests/f25WindowsUatRepairBatch11.test.js` propagation updated: async callbacks, `await validateBackendLaunchContract`, `assert.rejects` for async rejection cases.
- `CredentialVault` / `CredentialVaultHost` / lifecycle paths were moved to explicit async load/init/durability semantics.
- `createHydrationFrame`, `markHydrationAccepted`, vault `load/saveValues/replaceRaw`, host `initialize`, and coordinator `initialize` caller propagation was repaired across affected tests/tools.
- `credential-authority-lifecycle-matrix.js` async propagation was repaired:
  - both hosts initialized
  - hydration frame awaited before `.frame`
  - legacy vault explicitly loaded
  - negative cases catch `initialize()` rejection rather than constructor I/O failure
  - async rows are awaited before insertion
- `credential-authority-lifecycle-child.js` and multiple `tools/wp4` direct consumers were updated for the new async lifecycle.
- `CredentialCustodyHost.drain()` was added as a deterministic async barrier for already-consumed custody operations.
- Two earlier real P0 propagation bugs were fixed:
  - missing await on `markHydrationAccepted`
  - missing `await sourceVault.load()` before reading migrated legacy entries

Do not redo these changes just because a later sibling blocker is found.

## Evidence inheritance rule — mandatory

Useful prior work and test evidence must be retained. A new blocker does **not** invalidate the whole batch.

- A GREEN result remains valid if the production/test code it covers has not changed afterward.
- Only evidence whose covered code/path is modified by a later fix becomes stale and needs targeted rerun.
- Never rerun the entire credential suite merely because `BackendOwnerRegistry` or another sibling path changes.
- Never reset the worktree and thereby discard already-proven caller propagation or durability work.
- Keep a causal ledger: `evidence → covered code/path → later modifications → valid/stale`.

Known reusable evidence includes:

- `credential-vault-atomicity`: 5/5 PASS with WRITE / FSYNC / RENAME fault injection, fail-closed behavior, metadata indeterminate handling, restart recovery.
- Earlier focused GREEN evidence (reuse unless touched): backend launch preflight, owner admission/claim, phase2 recovery, containment recovery, platform identity, personal-access bootstrap, packaged startup frame, Windows cutover, unified mutation entry. If the relevant production path is modified again, rerun only the affected focused tests.
- A later broad credential run previously reported 87 total / 78 pass / 9 fail while caller propagation was still being repaired. Treat that run as diagnostic history, not a final gate and not a reason to rerun the whole suite repeatedly.

## Current blocker discovered by exact-head source audit

At checkpoint `810799ba`, `BackendOwnerRegistry` was not fully closed on the boot-critical async path.

Concrete source findings:

- `acquireStartupAdmission()` enabled path still used synchronous directory creation (`mkdirSync`).
- The enabled admission `register` path still used synchronous `_write(...)` / `atomicWriteJson(...)` rather than `_writeAsync(...)`.
- `BackendProcessHost` boot flow called synchronous owner state persistence for `STARTING` / `RUNNING` through `ownerRegistry.update(...)`.
- Actual ordering at checkpoint was approximately:
  `acquire admission lock → physical fork → capture process identity async → durable SPAWNED register → release admission`.
  This contradicts an earlier verbal report that durable owner registration happened before fork. Do not repeat that incorrect claim.

### Local work currently in progress after `810799ba`

The active executor is already editing the existing Windows worktree to close this exact owner-registry sub-batch. Screenshot/status showed work on:

- `electron/desktopHost/BackendOwnerRegistry.js`
  - asyncifying the known boot-critical admission/write surfaces
  - auditing async propagation into `acceptBackendOwner`, `persistRejectedOwnerMarker`, `clearRejectedOwner`, `containRejectedOwner`, and related containment/recovery callers
- `electron/desktopHost/BackendProcessHost.js`
  - replacing boot-path `STARTING` / `RUNNING` sync owner updates with awaited async updates
- `DesktopHost.js` and `DesktopCredentialApplicationCoordinator.js`
  - being read to verify containment/recovery propagation rather than blindly changing every caller

These post-checkpoint edits are LOCAL/IN-PROGRESS until a later commit is explicitly reported. Do not assume they are present in remote `810799ba`, and do not overwrite them by moving the checkpoint branch or resetting the worktree.

## Required owner-registry closure

Batch all same-root owner changes before validation:

1. `acquireStartupAdmission()` boot-reachable filesystem work must be async/awaited.
2. Enabled admission `register` must perform durable async write and remain fail-closed.
3. Add/use `updateAsync` (or equivalent) for boot-critical `STARTING` and `RUNNING` persistence and await it.
4. Audit rejection/containment/recovery owner persistence:
   - safety durability must not be fire-and-forget
   - async persistence must be propagated through callers
   - read-only memory snapshots can stay synchronous
5. Resolve and document the fork/owner invariant. If lock is intentionally held across fork until the PID-bound durable owner record is written, prove the crash window/recovery semantics. Do not fake a “durable-before-fork” claim if the architecture does not do that.
6. Assess boot-reachable `process.kill(pid, 0)` native sync liveness probes and either isolate/asyncify or explicitly justify a bounded allowlist based on reachability and evidence.

## Remaining closure work after owner batch

Do not jump to final CI before these are complete:

- Coordinator initialize tests:
  - constructor is pure-memory
  - concurrent initialize is idempotent
  - initialize failure is fail-closed; dangerous APIs reject afterward
- Deterministic NON-BLOCKING heartbeat tests for slow durability/boot operations.
- Durability-before-side-effect ordering tests for credential authority/vault and owner paths.
- Create/complete `tests/wp0/v21-boot-critical-path-event-loop-p0.test.js`.
- Perform explicit source reachability audit from `runtime-identity-verified` to physical backend fork, recording function, awaited barrier, remaining sync primitive, and allowlist reason.
- Guard must reject real boot-reachable `spawnSync`, `execSync`, `fs.*Sync`, `Atomics.wait`, busy waits/non-yield polling, and synchronous durable writers. Avoid naive scans of unrelated pre-marker/test-only code.
- `safeStorage.encryptString/decryptString` may remain only as an explicit bounded OS credential-security allowlist unless contrary evidence appears.
- Perform async P0 review: async callbacks inside map/filter/some/every, EventEmitter rejected promises, Electron IPC awaiting, catch/finally awaits, exit/error persistence, rejected `_initPromise`, hydration Promise shape, and concurrency failure semantics.

## Validation discipline from here

- Finish all known same-root owner-registry/startup-ordering edits first.
- Run only affected focused tests after batching changes:
  - owner admission / owner claim / owner recovery
  - BackendProcessHost startup/preflight/ordering
  - relevant heartbeat/order tests
  - marker→fork regression guard
- Reuse prior credential/lifecycle GREEN evidence unless those production paths are modified by the current owner fix.
- Before a production candidate: `node --check` on modified JS, `git diff --check`, `git status --short`, source audit, autonomous exact-head review P0=0/P1=0 if CodeRabbit unavailable.
- No CI while the local causal batch is still changing.
- Once local closure is real: one new production exact head → one necessary CI validation → ordinary merge if eligible → one new Windows RC/Product Final.

## Historical release evidence (do not rerun)

- PR #910 is frozen historical evidence only.
- Frozen exact head: `1918d9ae18edf20ea585f27948983c1c6ffacd32`.
- Product Final run: `33090872542`.
- PASS: unique full packaged build, real packaged/post-install launch, backend ready, Element reproducibility, Matrix UAT.
- Only fail: same-build startup capsule `STARTUP_CAPSULE_POST_INSTALL_TIMEOUT`.
- Diagnostics artifact id: `9654907032`.
- Last reliable startup checkpoint: `runtime-identity-verified`.

## New-chat continuation rule

When a new chat/session continues this project, first read this file plus the live checkpoint/production branch state. Do **not** restart diagnosis from historical PR #910, do not discard prior GREEN evidence, and do not assume `810799ba` is the latest local state. Continue from the current exact head/worktree using the evidence-inheritance rule above.
