# 言策发布上线收尾 — 2026-08-28 Durable Handoff

> Persistent execution handoff for project `言策发布上线收尾`.
> Branch: `memory/v21-boot-critical-20260828`
> Path: `memory/2026-08-28.md`
> This branch is handoff-only, not a production candidate, and must not be merged by itself.

## Mandatory execution model

- Fast Landing / batch causal closure only:
  shortest causal path → aggregate all same-root fixes → one new exact head validation → ordinary merge immediately if green → next real Windows RC/UAT.
- Same exact head NEVER reruns the same CI.
- Existing workflow/job logs/status are read-only evidence; never rerun to confirm/wait/get lucky.
- No squash / rebase / amend / force-push / cherry-pick / history rewrite.
- failure-first / fresh causal RED.
- unknownBlockers=0.
- exact-head gates.
- If CodeRabbit is unavailable/pending, ChatGPT autonomous independent exact-head review is allowed; P0=0 / P1=0 required.
- release / publish / ledger / final UAT remain fail-closed.
- UAT/RC large files default Motrix with a fresh GitHub signed URL.
- Real P0/P1 RED freezes that exact head permanently as evidence only; no rerun/mutate/reuse.

## Current authoritative release state

Repository: `laiqian0239-glitch/yance`

### Frozen successor-v8 RC — PR #931

- PR #931 ordinary-merged to main.
- Frozen exact RC head: `97a6a0d6bb6adfa12d1bcb210206ba3bf9b2f28b`.
- Product Final run: `33148877481` (one attempt only; never rerun).
- CI evidence on that run was green for packaged build/launch, startup capsule, Element reproducibility and Matrix artifact production.
- Desktop UAT artifact used locally:
  - id `9677223284`
  - SHA256 `26a6fc5435ddb8b71e38ac71d0246e5dd4392753fa5a87e208bf7939d453f2f0`
- Matrix UAT artifact used locally:
  - id `9677056729`
  - SHA256 `e1e24013107f1e2dafa56d1e02ac6af543aa195aa8e7e904dd51a28b99573315`
- Both downloads were locally SHA256-verified.

### Real Windows UAT RED discovered from frozen #931 artifacts

The Windows materialized UAT exposed one real release-blocking same-root defect. This supersedes the earlier apparent GREEN and freezes the old artifact set as evidence only.

Observed real failures:

1. `materialized-matrix-compose.yml` omitted the Synapse registration-secret bind required by the repository source topology:
   `/run/secrets/yance_matrix_registration_shared_secret`.
2. The sealed Matrix artifact omitted the existing `mautrix-meta` registration/data/runtime topology already present in `services/matrix/docker-compose.yml`.
3. `RUN_PRODUCT_EXPERIENCE_MATERIALIZED_UAT.ps1` accepted `docker compose up --no-build` process creation without proving real Synapse/required Matrix readiness before final evidence.
4. Local proof: Synapse exited with
   `FileNotFoundError: /run/secrets/yance_matrix_registration_shared_secret`, while Element could still remain up; therefore compose-start was a false-green boundary.
5. A prior printed local `PASS - Synapse is REALLY reachable` was not valid evidence because it executed after a thrown error in an interactive PowerShell session. Never reuse that printed PASS.

Frozen #931 exact head/artifacts must NOT be rerun or reused as final UAT.

## Current P0 authorization — PR #932

PR: `#932 [V21][P0] authorize materialized Matrix UAT runtime closure`

- state: OPEN
- mergeable: TRUE
- base: `main`
- trusted base SHA: `959fcff6ad7f14ea7520fd77b294eef5b1c11168`
- authorization branch: `governance/v21-final-rc-materialized-matrix-uat-runtime-closure-p0-authorization`
- current exact head: `dc7c1da50f593dec8202299729dec653a72dbb3b`
- exact diff: one governance authorization file only.

Exact-head existing CI evidence for `dc7c1da...` — DO NOT RERUN:

- Stage 6.4.5.9 WP0 Architecture Gates `33162323795` → PASS
- Layered CI Fast Feedback `33162323931` → PASS
- ACV2 WP-A Architecture Gates `33162323781` → PASS
- V21 Model Brain P0 Windows `33162323827` → PASS
- V21 Product Experience Shell P0 Final Validation `33162323869` → SKIPPED by governance routing, expected

The authorization already records OSS-first evidence and explicitly forbids new general-purpose infrastructure. It reuses:
- existing Yance Matrix source runtime topology,
- pinned upstream mautrix-meta runtime,
- existing Product Final image-only sealed UAT seam,
- official pinned Synapse runtime.

### Authorized one-batch P0 root fix after #932 ordinary merge

Implementation branch authority:
`fix/v21-final-rc-materialized-matrix-uat-runtime-closure-p0`

Allowed implementation paths are limited to:

- `.github/workflows/v21-product-experience-shell-p0-final-validation.yml`
- `tests/layered-ci/v21-product-experience-shell-p0-final-validation.test.js`
- `tests/wp0/v21-product-experience-materialized-uat.test.js`
- `tools/product-experience/RUN_PRODUCT_EXPERIENCE_MATERIALIZED_UAT.ps1`
- `tools/product-experience/materialized-matrix-compose.yml`

Required causal order:

1. ordinary-merge #932 if exact-head gates remain green and main has not drifted;
2. implementation branch must start from that authorization ordinary merge commit;
3. first implementation commit is tests-only failure-first RED in `tests/wp0/v21-product-experience-materialized-uat.test.js`;
4. RED must cover the complete same-root gap with unknownBlockers=0;
5. then one cohesive production batch:
   - keep Windows UAT image-only / `--no-build`;
   - build/export already-pinned `mautrix-meta` image in trusted Product Final CI;
   - seal existing mautrix-meta config/topology into Matrix artifact;
   - restore Synapse registration-secret bind and mautrix-meta registration/data/runtime topology;
   - create ephemeral per-UAT secrets outside the sealed artifact; never place plaintext secrets in evidence;
   - require real Synapse and required Matrix readiness before packaged Desktop launch/final evidence;
   - preserve exact Desktop/Matrix `candidateCommit` / `candidateTree` / sealed-artifact identity checks;
6. aggregate all fixes before one new exact-head CI validation;
7. ordinary merge immediately if green;
8. build a fresh RC and run real Windows UAT from fresh Desktop + Matrix artifacts;
9. release/publish/ledger remain fail-closed until that real Windows UAT is green.

Do NOT build Matrix source or native dependencies on the user's Windows UAT machine.
Do NOT invent a second Matrix topology, custom Synapse wrapper, replacement homeserver, secret service, or new UAT orchestrator.

## Brand / UX decision — queued immediately AFTER P0 closure

This is a locked product decision so it is not forgotten across chats, but it MUST NOT be mixed into the current Matrix P0 causal batch.

### Chosen login layout

- User selected the dual-column login concept (方案 C layout structure).
- Scenic/photo wallpaper is rejected and must be removed.
- Final layout target:
  - left: brand panel / logo / `言策` / `YANCE` / concise brand line;
  - right: existing login/create-account/homeserver/language controls;
  - existing Matrix/Element auth state machine stays authoritative.
- Use mature OSS login-shell/layout source where suitable; no new self-built authentication framework or general-purpose UI library.

### Chosen new brand direction

User selected **方案 B：高端紫金**.
Current green swirl/refresh-like logo is rejected for final branding.

Target palette / design tokens:

- Midnight: `#0D1022` — main dark background
- Aubergine: `#24143D` — brand purple / left brand panel
- Electric Violet: `#6D31F5` — AI/focus/highlight, limited use
- Soft Gold: `#D9B56B` — logo / primary CTA / key brand accent
- Mint Mist: `#BDE9DF` — restrained success/support accent
- Warm White: `#F5F2EA` — primary light text
- Muted Lavender: `#AAA4B7` — secondary text

Logo direction:
- replace swirl with a geometric dialogue + strategy/node/path emblem;
- primary mark can use gold on deep purple/midnight;
- dedicated micro/tray mark required for 16/20/24px readability; do not merely scale down the full mark;
- desktop/app icon: deep purple/navy base + simplified gold emblem.

Existing repository branding pipeline MUST be reused, not replaced:
- `scripts/branding/build-yance-assets.js`
- `scripts/branding/verify-yance-assets.js`
- existing master/product/micro/mono SVG structure
- deterministic PNG sizes 16→1024 and ICO generation.

Current brand is still brandingVersion 2 and old blue-green tokens. Brand batch must update tokens + verifier + source/product/micro/mono assets together as one causal batch so old hard-coded color checks do not create meaningless RED.

Brand/UX work priority: P1 release-experience closure AFTER Matrix P0 and fresh real Windows RC/UAT. Do not mix it into #932/P0 implementation.

## New-chat continuation rule

On every new chat/session:

1. Read `memory/v21-boot-critical-20260828:memory/2026-08-28.md` first.
2. Treat this file as the current durable handoff and prefer it over older project summaries.
3. Check live PR #932 exact head/status read-only; never rerun existing exact-head CI.
4. Finish Matrix P0 first using Fast Landing.
5. After fresh real Windows RC/UAT passes, immediately open the separate OSS-first brand/UX batch using the locked **高端紫金 + 双栏登录** decision above.
6. Never regress to the old green-swirl logo or scenic-wallpaper login concept unless the user explicitly changes the decision.
