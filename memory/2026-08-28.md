# 言策发布上线收尾 — V21 Boot Critical Path Durable Handoff

> Durable project memory / execution handoff. Keep this file on the separate `memory/v21-boot-critical-20260828` branch. It must not be treated as a production candidate or merged by itself.

## Hard execution rules

- Fast Landing = shortest causal path → aggregate same-root fixes → one new exact head → one necessary CI validation → ordinary merge immediately if eligible → next Windows RC/UAT.
- Same exact head must never rerun the same CI. Existing workflow/job logs/status are evidence; do not rerun for reassurance.
- No squash, rebase, amend, force-push, cherry-pick, or history rewrite.
- Frozen historical PRs are evidence only. Release/publish/ledger/final UAT remain fail-closed.
- UAT/RC large files default to Motrix using a fresh GitHub signed URL.
- Do not claim final root-cause closure until the full marker→fork graph is enumerated, focused RED→GREEN is complete, the regression guard is clean, and a new same-build Windows startup capsule passes.

## Repository / worktree

- Repo: `laiqian0239-glitch/yance`
- Active Windows worktree: `C:\GitHub\yance-boot-critical-async-durability`
- Alternate dependencies if needed: `C:\GitHub\yance-electron-lifecycle\node_modules`
- Set `NODE_PATH=C:\GitHub\yance-electron-lifecycle\node_modules` when the active worktree has no `node_modules`.
- Baseline parent for this causal batch: `d5a5e8ff1f41352a2f400ed37138ea14ef820484` (#909 ordinary merge).

## Remote safety checkpoints

All of these are disaster-recovery checkpoints only. None is a production candidate or a freeze/reset point.

1. `93b9df438c25c9eff1b8a414e3425467600ca714`
   - branch: `checkpoint/v21-boot-critical-async-20260828`
   - first safety checkpoint

2. `810799ba1eb7b45cf9d4b47b7b717754c1027b5a`
   - same checkpoint branch, ordinary fast-forward from `93b9df43`
   - commit: `checkpoint(v21): preserve boot-critical async propagation round 2 [skip ci]`
   - 30 tracked files saved, unrelated files = 0

3. `0b491b269e7d577278d59de2791bf7ac7734fbcc`
   - same checkpoint branch, ordinary fast-forward from `810799ba`
   - parent is exactly `810799ba1eb7b45cf9d4b47b7b717754c1027b5a`
   - commit: `checkpoint(v21): preserve owner async closure progress [skip ci]`
   - 3 tracked files saved, unrelated files = 0
   - files:
     - `electron/desktopHost/BackendOwnerRegistry.js`
     - `electron/desktopHost/BackendProcessHost.js`
     - `electron/desktopHost/DesktopCredentialApplicationCoordinator.js`
   - remote branch verified at this exact SHA
   - GitHub workflow runs for this exact SHA: none observed

Never reset/restore the active worktree back to any checkpoint merely because it is remote-safe. Continue incrementally from the current worktree. The checkpoint protects already-completed work from loss.

Six local diagnostic scripts are intentionally untracked and are not deliverables:

- `diag-genesis.js`
- `diag-prepare.js`
- `diag-prepare2.js`
- `diag-timing.js`
- `diag-write-error.js`
- `diag-write-error2.js`

Do not commit them merely to make the tree clean. Preserve them locally until no longer needed for causal diagnosis.

## Completed and reusable work through checkpoint `810799ba`

These changes are valuable and must be preserved unless a later concrete failure proves one of them wrong:

- `BackendProcessHost` trusted Node runtime probe: sync process probing replaced by bounded async `execFile` path.
- Boot-reachable launch validation asyncified:
  - `pathExists` uses async access
  - `firstExistingDelimitedPath` uses `for...of` + `await` and never `Array.find(async ...)`
  - `validateReleaseStartupConfig` async
  - `validateBackendLaunchContract` async
  - `_startUnlocked` awaits validation
- `backend/tests/f25WindowsUatRepairBatch11.test.js` propagation updated: async callbacks, `await validateBackendLaunchContract`, `assert.rejects` for async rejection cases.
- `CredentialVault` / `CredentialVaultHost` / lifecycle paths moved to explicit async load/init/durability semantics.
- `createHydrationFrame`, `markHydrationAccepted`, vault `load/saveValues/replaceRaw`, host `initialize`, and coordinator `initialize` caller propagation repaired across affected tests/tools.
- `credential-authority-lifecycle-matrix.js` async propagation repaired:
  - both hosts initialized
  - hydration frame awaited before `.frame`
  - legacy vault explicitly loaded
  - negative cases catch `initialize()` rejection rather than constructor I/O failure
  - async rows are awaited before insertion
- `credential-authority-lifecycle-child.js` and multiple `tools/wp4` direct consumers updated for the new async lifecycle.
- `CredentialCustodyHost.drain()` added as a deterministic async barrier for already-consumed custody operations.
- Two earlier real P0 propagation bugs fixed:
  - missing await on `markHydrationAccepted`
  - missing `await sourceVault.load()` before reading migrated legacy entries

Do not redo these changes merely because a sibling blocker is found.

## New owner async work preserved at checkpoint `0b491b2`

This checkpoint is partial progress in the same owner-registry causal sub-batch. It is valuable and must be carried forward, but it is not yet proof of closure.

### `BackendOwnerRegistry.js`

The checkpoint contains these concrete changes:

- imports/uses async `mkdirAsync` for boot-reachable admission directory creation instead of `mkdirSync`
- enabled `acquireStartupAdmission().register` is now `async`
- enabled admission registration now awaits `_writeAsync(...)` instead of synchronous `_write(...)`
- added `updateAsync(...)` backed by `_writeAsync(...)`
- added `markRejectedAsync(...)`
- added `markExitedAsync(...)`

Important: synchronous compatibility methods such as `_write`, `register`, `update`, `markRejected`, `markExited`, and other sync helpers still exist in the source. Their mere presence is not automatically a boot blocker, but every real marker→fork / startup / containment caller must be reachability-audited. Do not assume closure from method names alone.

### `BackendProcessHost.js`

The checkpoint contains these concrete async-propagation changes:

- boot-path owner `STARTING` persistence now awaits `ownerRegistry.updateAsync(...)`
- boot-path owner `RUNNING` persistence now awaits `ownerRegistry.updateAsync(...)`
- rejection/recovery paths now await `clearRejectedOwner(...)` where converted async
- orphan-owner termination paths now await `ownerRegistry.markExitedAsync(...)`
- `acceptBackendOwner(...)` converted async and awaits durable `updateAsync(...)`
- `persistRejectedOwnerMarker(...)` converted async and awaits `markRejectedAsync(...)`
- `clearRejectedOwner(...)` converted async and awaits durable `updateAsync(...)`

### `DesktopCredentialApplicationCoordinator.js`

The checkpoint propagates the newly async owner operations into coordinator paths:

- containment persistence awaits `desktopHost.persistRejectedBackendOwner(...)`
- containment release awaits `desktopHost.clearRejectedBackendOwner(...)`
- runtime projection acceptance awaits `desktopHost.acceptBackendOwner(...)`
- already-ready acceptance path also awaits `acceptBackendOwner(...)`

These changes are preserved remotely and must not be discarded on session change.

## Evidence inheritance rule — mandatory

Useful prior work and test evidence must be retained. A new blocker does **not** invalidate the whole batch.

- A GREEN result remains valid if the production/test code/path it covers has not changed afterward.
- Only evidence whose covered code/path is modified by a later fix becomes stale and needs targeted rerun.
- Never rerun the entire credential suite merely because `BackendOwnerRegistry`, `BackendProcessHost`, or another sibling path changes.
- Never reset the worktree and thereby discard already-proven caller propagation or durability work.
- Keep a causal ledger: `evidence → covered code/path → later modifications → valid/stale`.

Known reusable evidence includes:

- `credential-vault-atomicity`: 5/5 PASS with WRITE / FSYNC / RENAME fault injection, fail-closed behavior, metadata indeterminate handling, restart recovery.
- Earlier focused GREEN evidence exists for backend launch preflight, owner admission/claim, phase2 recovery, containment recovery, platform identity, personal-access bootstrap, packaged startup frame, Windows cutover, unified mutation entry. Reuse each only if its covered production path has not changed since that run.
- Because checkpoint `0b491b2` modifies owner admission/state persistence, BackendProcessHost owner lifecycle, and coordinator containment/acceptance propagation, prior GREEN evidence specifically covering those changed paths is now stale and must be rerun as an affected focused set.
- Credential-vault/lifecycle/caller-propagation evidence that does not cover these three modified owner/coordinator files remains reusable.
- A prior broad credential run reported 87 total / 78 pass / 9 fail while caller propagation was still being repaired. Treat it as diagnostic history, not a final gate and not a reason for another broad rerun.

## Current exact state after checkpoint `0b491b2`

Remote safety branch exact head:

`0b491b269e7d577278d59de2791bf7ac7734fbcc`

The commit is one ordinary fast-forward commit after `810799ba`. No PR, no main push, no production head, and no CI validation should be inferred from this checkpoint.

The checkpoint captures the owner async edits made immediately before executor quota exhaustion. It does **not** establish that all async caller propagation, event-handler safety, containment/recovery durability, or fork/owner crash-window invariants are complete.

## Required next source audit — do this before broad tests

Continue from `0b491b2` / current worktree and finish the owner-registry causal batch before validating it.

1. Audit `DesktopHost.js` wrappers/callers for the newly async methods:
   - `acceptBackendOwner`
   - `persistRejectedBackendOwner`
   - `clearRejectedBackendOwner`
   - any wrapper around `persistRejectedOwnerMarker`, `clearRejectedOwner`, `containRejectedOwner`
   Ensure every caller that depends on durability actually awaits the Promise.

2. Audit all `BackendProcessHost` internal callers after async conversion:
   - `acceptBackendOwner`
   - `persistRejectedOwnerMarker`
   - `clearRejectedOwner`
   - `_terminateOrphanOwner`
   - owner exit/recovery callbacks
   - catch/finally cleanup paths
   Do not leave unhandled/rejected Promises in EventEmitter callbacks.

3. Audit owner exit handler(s): if the callback itself cannot be async-safely awaited by EventEmitter, attach an explicit rejection handler and preserve fail-closed containment semantics. Do not silently fire-and-forget safety durability.

4. Audit coordinator containment methods end-to-end:
   - `_engageRejectedOwnerContainment`
   - `_verifyRejectedOwnerEnforcement`
   - rejected-owner persistence/clear
   - cleanup paths
   Ensure persistence completes before state is treated as durable/cleared.

5. Audit remaining sync owner methods by reachability, not by grep count. Production marker→fork/startup/recovery paths must not fall back into `_write(...)`, `atomicWriteJson(...)`, `mkdirSync`, or other sync fs through a missed caller.

6. Resolve and document the fork/owner invariant. Current architecture at `810799ba` was approximately:
   `acquire admission lock → physical fork → capture process identity async → durable SPAWNED register → release admission`.
   If this lock-held-across-fork design remains, prove the parent-crash window/orphan recovery semantics. Do not falsely describe it as durable-before-fork.

7. Assess boot-reachable `process.kill(pid, 0)` / synchronous native liveness probes. Async/isolate if materially boot-reachable; otherwise document a bounded explicit allowlist with evidence.

## Focused validation after the owner batch is fully edited

Do not run a broad credential suite first. Batch all known same-root edits, then run only the affected focused set:

- owner admission / claim lock / owner recovery
- BackendProcessHost startup / owner ordering / preflight tests affected by the new async methods
- coordinator containment/recovery/acceptance tests affected by the new awaits
- deterministic heartbeat/order tests for the changed durability path
- marker→fork regression guard once added

If an affected test fails, classify before editing:

- async caller propagation → fix await/helper propagation
- fixture/fault-injection incompatibility → fix test abstraction, not safety semantics
- genuine production invariant failure → fix production in the same causal batch
- unrelated/static/environment noise → isolate, do not expand scope

## Remaining closure work after owner batch

Do not jump to final CI before these are complete:

- Coordinator initialize tests:
  - constructor pure-memory
  - concurrent initialize idempotent
  - initialize failure fail-closed; dangerous APIs reject afterward
- Deterministic NON-BLOCKING heartbeat tests for slow durability/boot operations.
- Durability-before-side-effect ordering tests for credential authority/vault and owner paths.
- Create/complete `tests/wp0/v21-boot-critical-path-event-loop-p0.test.js`.
- Explicit source reachability audit from `runtime-identity-verified` to physical backend fork, recording function, awaited barrier, remaining sync primitive, and allowlist reason.
- Guard must reject real boot-reachable `spawnSync`, `execSync`, `fs.*Sync`, `Atomics.wait`, busy waits/non-yield polling, and synchronous durable writers. Avoid naive scans of unrelated pre-marker/test-only code.
- `safeStorage.encryptString/decryptString` may remain only as an explicit bounded OS credential-security allowlist unless contrary evidence appears.
- Async P0 review: async callbacks inside map/filter/some/every, EventEmitter rejected promises, Electron IPC awaiting, catch/finally awaits, exit/error persistence, rejected `_initPromise`, hydration Promise shape, and concurrency failure semantics.

## Final validation / Git / CI discipline

Before a production candidate:

- `node --check` on modified JS
- `git diff --check`
- `git status --short`
- complete marker→fork source audit
- autonomous exact-head review P0=0/P1=0 if CodeRabbit unavailable

No CI while the causal batch is still changing.

Once local closure is real:

`one new production exact head → one necessary CI validation → ordinary merge if eligible → one new Windows RC/Product Final`

Never rerun the same exact head CI.

## Historical release evidence — do not rerun

- PR #910 is frozen historical evidence only.
- Frozen exact head: `1918d9ae18edf20ea585f27948983c1c6ffacd32`.
- Product Final run: `33090872542`.
- PASS: unique full packaged build, real packaged/post-install launch, backend ready, Element reproducibility, Matrix UAT.
- Only fail: same-build startup capsule `STARTUP_CAPSULE_POST_INSTALL_TIMEOUT`.
- Diagnostics artifact id: `9654907032`.
- Last reliable startup checkpoint: `runtime-identity-verified`.

## New-chat continuation rule

When a new chat/session continues this project:

1. Read this file first: `memory/v21-boot-critical-20260828:memory/2026-08-28.md`.
2. Read the live `checkpoint/v21-boot-critical-async-20260828` branch and confirm its current exact SHA.
3. Treat `0b491b269e7d577278d59de2791bf7ac7734fbcc` as the latest known remote safety checkpoint unless a later handoff updates this file.
4. Do not restart diagnosis from historical PR #910.
5. Do not reset/restore the active worktree to a checkpoint if newer local edits exist.
6. Preserve all reusable GREEN evidence using the evidence-inheritance rule.
7. Continue first with the owner async caller/recovery audit above; do not waste time rerunning unrelated credential suites.
