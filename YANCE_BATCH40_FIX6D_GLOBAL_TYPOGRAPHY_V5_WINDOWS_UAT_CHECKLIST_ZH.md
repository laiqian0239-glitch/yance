# FIX6D 全局排版 V5 Windows 实机复验清单

## 基线门禁

- 校验源码交付包 SHA256；不一致立即停止。
- 确认状态：`windowsUiUat=false`、`readyForPromotion=false`、`formalRelease=false`。
- 不使用任何旧 FIX6D 候选包。

## 必测矩阵

- Windows 缩放：100%、125%、150%。
- 窗口：普通、窄窗口。
- 字号：标准、舒适、大字。
- 密度：紧凑、舒适。
- 导航：展开、紧凑、隐藏。
- AI 面板：打开、关闭。
- 正式路由：会话、联系人、客户档案、关系时间线、关系洞察、AI 工作台、账号中心、系统中心、设置恢复、主题外观。

## 每场景核验

- 标题、正文、说明、状态、标签、数据值、按钮均随字号变化；
- 不出现裁切、遮挡、文字纵排、固定单行省略或隐藏说明；
- 控件自然增高且文字完整；
- 不出现页面滚动与内部滚动竞争；
- 双栏时左侧主列表填满剩余高度，窄屏时自然重排；
- 标题滚动定位后不贴住或进入 Windows 标题栏；
- AI 面板开关不改变主工作区可用高度；
- Persona 不出现 7px/10px 等微型文本；
- 通知位于标题栏下方且不覆盖 Windows 窗口控件；
- 主题与外观页、系统设置页、账号中心均可完整滚动；
- 旧修复项：空状态、导航、编辑器图标、界面密度按钮保持通过。

## 证据要求

- 每档缩放保存环境信息、窗口尺寸和完整矩阵结果；
- 失败必须保存截图、路由、字号、密度、导航、AI 状态和复现步骤；
- 所有失败闭环并复验前，不得生成正式候选包。
