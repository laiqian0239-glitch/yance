# Yance V2.1 Presence / Avatar P0 V1 V3 — Implementation Plan

1. Keep the failure-first commit as immutable first implementation history: exact five frozen Presence tests, sole parent the V3 authorization merge.
2. Pin mature OSS authority and licenses; add exactly `livekit-client: 2.21.0` to the Element module manifest.
3. Add a thin Electron `presenceAvatarRuntime` that validates endpoints and Voice AudioChunk metadata, projects the native CyberVerse Character catalog, creates/deletes CyberVerse sessions, and forwards external audio without resampling or WebRTC/avatar logic.
4. Refactor pinned CyberVerse internally so the existing non-silent standard TTS avatar AV block becomes one shared `runAvatarAVDriver`; standard TTS and `/api/v1/sessions/{id}/external-audio` both call that implementation. Keep external streams keyed by session+reply and session teardown authoritative. Do not duplicate `GenerateAvatarStream`, AV sync, pacing, or MediaPeer publishing in Yance or a second CyberVerse loop.
5. Register Presence through the existing M2 IPC manifest and `ipcGuardHandle`; preload exposes only health/session/audio methods and no LiveKit secret/signing material. Reuse existing application runtime shutdown authority to close all active CyberVerse sessions on quit/relaunch.
6. Reuse the existing Element global-right-panel workspace. Select real CyberVerse Characters, use the official `livekit-client` Room for connect/mic/camera lifecycle, attach subscribed remote tracks with `Track.attach()` / `Track.detach()`, fence stale Room events, and recover browser-blocked remote audio with the official `Room.startAudio()` user-gesture path.
7. Add CyberVerse service/model YAML and a Windows gate. Do not package CyberVerse GPU/CUDA runtime or SoulX weights in Electron.
8. Re-run the five Presence contracts, Stage, Layered, ACV2, PVEP as applicable, and the dedicated Windows gate. Fix every real RED at the owning layer; never weaken tests/gates.
9. Reconcile forward from fresh trusted main if a sibling workline merges a shared root first. In particular, when Voice Brain is present on main, audit the real Voice AudioChunk producer-to-Presence boundary rather than creating a second Voice adapter. Never rebase, force-push, amend, squash, or reuse invalid V2 implementation history.
10. Complete an independent exact-Head review with zero P0/P1 findings, then stop at the final implementation owner merge boundary.
