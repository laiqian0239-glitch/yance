# Yance V2.1 Graphiti P0 v1 Implementation Plan

## Authority and scope

This work package implements the already-merged `V21-GRAPHITI-P0-V1` authorization from `main@152b2f91c9b7a09d06ecdbebd385b6b2045d7a4e`. It does not reopen authorization, alter Letta or Parlant authority, or authorize release/publish/promotion.

The implementation branch is `product/v21-graphiti-p0-v1`. The final diff is restricted to the 20 authorized exact paths with canonical path-set SHA-256 `9dcc1cc0e91626f977a14b324e285ec5c8526c93bb54f37518e71ed520e4499b`.

## Failure-first sequence

1. Start from the ordinary two-parent authorization merge commit.
2. Land a test-only RED commit that proves missing Graphiti runtime, missing portable seal, missing broker-owned projection write path, and competing legacy relationship inference projection.
3. Verify Neo4j Community 2026.07.1 first-party checksum before sealing. The accepted first-party SHA-256 is `d70f2019c7a53b6ed5ac61a027a9884a5dbcf714d52e941249036d02d7886162` from `dist.neo4j.org`.
4. Implement only thin Graphiti-specific lifecycle, credential, projection and sealing adapters around mature upstream OSS.
5. Require exact-Head Stage/Layered/ACV2/Windows runtime gates and independent review before merge.

## OSS ownership

- Graphiti `v0.29.3` at `021d3a57d511f21b10adaf7fa923bd5c1fce5e9d` owns the temporal relationship graph, episode provenance, validity/invalidation and supersession. Graphiti upstream calls extracted edges `facts`; Yance treats automatically extracted Graphiti edges as AI inference until the user explicitly confirms or supplies the fact.
- Neo4j Community `2026.07.1` is the local Graphiti graph store.
- Letta remains long-term person/interaction memory authority.
- Parlant remains Goal/Journey authority.
- Yance remains final reply decision and unique send authority.
- Yance does not implement a temporal graph, supersession algorithm, graph database, model gateway, general Python runtime framework or general sidecar framework.

## Runtime boundary

Electron owns the Graphiti and Neo4j child lifecycle. Both listeners are loopback-only. Neo4j HTTP/HTTPS are disabled; authenticated Bolt is the only database connector. Graphiti HTTP requires a per-runtime ownership token and challenge proof.

OpenRouter credentials stay in the existing Yance credential authority. A high-entropy Neo4j password is generated by Electron main and persisted only through the existing encrypted CredentialVault custody host before backend startup. Renderer state, logs and plaintext Yance files never receive either secret.

Each inbound relationship message is admitted with Graphiti native `add_episode` using a deterministic relationship `group_id`, source provenance and reference time. Native Graphiti `search` recall is projected into the existing relationship timeline only through the workspace facade/service/`RelationshipKeyNodeRepository` authority-write-host chain. Direct Electron SQL and a second SQLite writer remain forbidden.

When Graphiti-provenance inference exists, `RelationshipProjectionAuthority` prefers the Graphiti temporal inference timeline over competing legacy inferred rows while preserving manual key-node annotations and explicit user confirmations. Graphiti extraction is never silently promoted to a Yance confirmed fact, and the absence of an upstream calibrated confidence is represented as unscored rather than a fabricated numeric certainty.

## Portable Windows seal

The Windows build uses exact verified inputs only:

- Graphiti exact tag/commit and exact upstream `uv.lock` Git blob `871ec1a85fbcfc80b3919f4178818301981e43e2`.
- uv `0.12.3` as build/sealing tool only.
- python-build-standalone CPython `3.12.13` asset `18bcc65b17921806b72cdc88bcf000bf67a2c99a8fc381fe1629f2b9ba56858d`.
- Eclipse Temurin `jdk-21.0.11+10` asset `d3625e7cadf23787ea540229544b6e2ab494b3b54da1801879e583e1dfee0a64`.
- Neo4j Community Windows ZIP `d70f2019c7a53b6ed5ac61a027a9884a5dbcf714d52e941249036d02d7886162`, after first-party `.sha256` equality and local `Get-FileHash` verification.

Build-time online materialization is followed by an `uv sync --frozen --offline` closure check. The shipped resource contains Python, the materialized Graphiti environment, Java, Neo4j, bridge, licenses, deterministic CycloneDX 1.7 SBOM and deterministic tree seal. It contains no uv executable, Git checkout, lock/source checkout, pip installer step or runtime download path.

The Windows workflow then starts Neo4j and Graphiti with external HTTP proxy resolution intentionally broken, verifies authenticated ownership health on `127.0.0.1:18766`, rejects unauthenticated relationship search, and reruns the Graphiti WP0 contracts.

## Merge boundary

Do not mark the PR ready or merge from cached evidence. Re-read fresh `main`, exact implementation Head, changed-path set/hash, Stage/Layered/ACV2/Windows workflow results and review threads. Merge must remain an ordinary two-parent merge and requires zero unresolved P0/P1 findings. Release, publish and promotion remain outside this work package.
