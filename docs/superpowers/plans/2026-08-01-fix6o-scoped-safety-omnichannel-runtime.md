# FIX6O Implementation Plan

1. Freeze FIX6N source identity and reproduce single-account/global-safe-mode regression.
2. Add scoped trigger normalization and global-reason allow list.
3. Persist scoped issues and append-only events in Schema 20.
4. Route runtime safety supervisor output through scoped isolation.
5. Replace force clearing with recovery preflight and one-time API v2 receipt.
6. Project scoped issues separately in System Center.
7. Split Facebook Page, personal identity, and personal Messenger driver contracts.
8. Add official personal identity OAuth mode to the existing Worker without returning access tokens to Windows.
9. Keep personal Messenger onboarding unavailable pending a real isolated browser bridge.
10. Run syntax, focused, full backend, UI/UAT, and source-delivery gates.
11. Regenerate derived source identity and package a Git-free Windows source UAT handoff.
12. Keep promotion and formal-release gates false until real Windows evidence is uploaded.
