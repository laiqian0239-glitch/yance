# Yance V2.1 Agent Lightning P1 Implementation Plan

1. Preserve the first implementation commit `37a55bcfbbfc18123874a802cf708e6ac4673d0f` as the seven-test-only causal RED and retain workflow run `31763470495` as failure-first evidence.
2. Add only the remaining twelve authorized paths in one direct child commit.
3. Adopt the exact Agent Lightning v0.3.0 lock and MIT notice/license evidence; do not alter root npm dependency controls.
4. Implement the Learning/Model Brain Node adapter and the sealed Linux Python APO runtime with work-package-local stdio IPC.
5. Keep Learning scores unchanged, storage run-scoped, model calls Model-Brain-owned and outputs `CANDIDATE_ONLY`.
6. Run the unchanged seven tests, Python syntax/self-check, SBOM/lock integrity checks and authorized-path seal before the post-RED commit.
7. Commit with `Yance-Failure-First-Red-Head`, `Yance-Failure-First-Red-Run` and `Yance-Failure-First-Red-Conclusion` trailers, then normal fast-forward push only.
8. Require exact-head CI and independent review with P0=0/P1=0. Re-read live main and frozen Learning/Model Brain/shared-notice overlaps before stopping at the ordinary merge boundary.
