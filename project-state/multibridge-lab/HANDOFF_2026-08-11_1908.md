# YANCE-MULTIBRIDGE-LAB — CONTINUATION HANDOFF

Created: 2026-08-11 19:08 +07:00
Branch: `lab/multibridge-recovery-plan-20260811`
Authoritative live ledger: `project-state/multibridge-lab/STATUS.md`
SSOT commit immediately before this handoff file: `100129092723667e9a2597d537a7b0164502180c`

> New chat rule: first re-fetch the live branch ref and re-read `project-state/multibridge-lab/STATUS.md`. Real-time GitHub is highest authority. Do not trust a cached Head if the branch moved after this handoff.

## User operating authorization

Continue independently without per-task approval inside the existing Lab safety boundary.

Mandatory rules:

- failure-first / TDD;
- no temporary workaround; fix root cause;
- mature OSS upstream source/runtime first;
- no new Yance infrastructure without OSS-fit;
- no force push, rebase, amend published history, squash, or weakened gates;
- Lab remains independent of Yance main;
- do not ask the user to perform basic script/config debugging;
- only stop for a real RED requiring new causal work, a hard human/account authorization boundary, or final integration/merge boundary;
- do not repeat completed WhatsApp or Telegram work;
- do not revive R13/R13.1/R13.2/R13.3.

## Frozen completed work — DO NOT REPEAT

### WhatsApp

- authority: mautrix-whatsapp v0.2607.0;
- exact SHA: `a86f5eb9bf7d5a4a6cc7a1c4e42d322bdcb03aa2`;
- PR #112 ordinary merged.

### Telegram

- real-device GREEN exact Head: `c85b03d37107a211075aece254c031ec5cff3586`;
- exact image: `sha256:e064d991e9aefb9eee3c0ecc9615e601c73687ea2f0d493730dddb3dd6403084`.

### Synapse

Exact pin/image/account state remains frozen from prior Lab work. Credentials remain local and must never be moved into GitHub artifacts.

## Current R12 runtime truth

R12 readiness remains revoked for exactly:

- `facebook-personal`
- `instagram-dm`
- `google-messages`
- `signal`
- `line`

Original Windows evidence showed all five at `restarting|11|243`.

Historical R12 generator root defect is now causally established: historical `Wire-BridgeConfig` wired homeserver/appservice/matrix/permissions but omitted `.database.type` and `.database.uri`.

Historical immutable fixture:

- commit `65a41976fdcb8d321fab92ac03c65cd647e822ab`;
- path `tests/multibridge-lab/fixtures/r12-wire-bridge-config-expression.txt`.

Do not reconstruct the whole old R12 script from memory. The fixture preserves only the exact previously verified relevant yq expression.

## Fatal-context collector — SEALED GREEN, DO NOT RERUN USER MACHINE NOW

Failure-first lineage:

- test-only `d2030943491994f5db46c89b2f7b1b73694ace3c`;
- implementation `88fd350e163319d536bea1f2fb35a5881752e972`;
- test representation correction `4022d807860ec5a7da9cd9704bfa3e587da44939`;
- final CI pin-only commit `4bd07b41451d2c27b7a2945bb08d76570d2ed543`.

Final authority:

- run `31485153849`;
- job `93758725677`;
- 15/15 tests GREEN;
- staging + both artifact uploads GREEN;
- artifacts independently downloaded and hash/source/file-set verified.

Collector path:

- `tools/multibridge-lab/collect-exit11-evidence.ps1`

Workflow:

- `.github/workflows/multibridge-lab-native-process.yml`

Do not ask the user to rerun this collector unless a new unexplained runtime RED remains after the five-service DB repair.

## Current DB repair implementation — IMPORTANT: STILL ONLY THREE TARGETS

Failure-first:

- test-only commit `645eb7a2429cb34f179e58fbab579ed3aaa994af`;
- Windows RED run `31485657849`, job `93760328914`;
- 16 prior tests GREEN / 2 targeted RED because implementation was absent.

Implementation:

- commit `63c008a31b8e36b093a7fc9f39d918f0960dc159`;
- path `tools/multibridge-lab/r12-database-wiring.ps1`;
- function `Get-LabR12DatabaseWiring`.

Current exact target set in implementation is still only:

1. `instagram-dm`
2. `google-messages`
3. `signal`

Current values:

- `Type = sqlite3-fk-wal`
- `Uri = file:/data/<service>.db?_txlock=immediate`
- yq fragment `.database.type=strenv(YANCE_DATABASE_TYPE)|.database.uri=strenv(YANCE_DATABASE_URI)`

Current implementation intentionally returns `$null` for Facebook and LINE. Do not silently edit this before a new failure-first scope expansion.

Windows implementation GREEN:

- run `31485835966`;
- job `93760893132`;
- 18/18 tests GREEN.

## Exact source-semantic authority for the original three targets

Immutable authority fixture:

- commit `cba12644cae7cd248bb25337df50bbb9799b2af1`;
- path `tests/multibridge-lab/fixtures/upstream-database-validator-authorities.json`.

Verification-only test:

- commit `cdd22bfc400b5e6967af3e8cb4b6cc248f3f7c3c`;
- path `tests/multibridge-lab/r12-upstream-database-validator.test.js`;
- Windows run `31486266961`, job `93762278784`;
- 20/20 GREEN.

Exact bridgev2 dependency authorities already frozen:

- Meta/Instagram: `56938b8a508d37c2501629d9b35538e849f4a63b`;
- Google Messages: `5743d9b6f27e2de4966f50e13a658308cdcdbbcb`;
- Signal: `f7cfa8766d2bcf45f944fc76ea856bcc36317ad9`.

All exact validators use the same fatal predicate: upstream Postgres placeholder URI → `database.uri not configured`. Exact examples support `sqlite3-fk-wal` and recommend `file:<path>?_txlock=immediate`.

## Exact pinned-image DB validation — THREE IMPLEMENTED TARGETS SEALED GREEN

Workflow:

- `.github/workflows/multibridge-lab-pinned-db-image-validation.yml`

Initial harness commit `2a1743a1856132c8552639928578e54a656cf74a` proved exact image builds but omitted historical R12 permissions and therefore hit a harness-only `bridge.permissions not configured` after clearing DB fatal.

Harness fidelity root repair:

- CI-only commit `8aaceef6b22d410c0f975c18ba46a0a9c6fc7ed0`;
- replays exact frozen historical R12 non-DB fields with safe dummy values;
- keeps exact upstream builds, exact binaries, `--network none`, isolated `/data`, and no-secret artifacts.

Repaired run `31487411606`:

### Instagram DM

- job `93765873058`;
- exact Meta commit `a0db68a56bb5715d67faa331f647e771d62b05a2`;
- exact `Dockerfile.ig`;
- exact image `sha256:fd83600ab2d55aa02f998067daf3fb8baa889874d5813f31a6c79a2a20bd669c`;
- state running / exit 0 / SQLite DB present / no Configuration error;
- artifact `9099701058`;
- digest `sha256:f582c497b98e5cde2dc9954b17aae94d14e84c711e7c206604f0cec62714dc23`;
- independently verified.

### Google Messages

- job `93765873115`;
- exact commit `2f2a1efa59a1bfbfb0ab1570b0532a93baeeea96`;
- exact image `sha256:87e2bf3d75cb2d201958104a98e4d84d80dfc770918211f1213b6d034a4b1b16`;
- state running / exit 0 / SQLite DB present / no Configuration error;
- artifact `9099710796`;
- digest `sha256:b85260a3b9750822f8837f010d48476ad3c6c24854993b8ae6f04bcaddd374e1`;
- independently verified.

### Signal

- job `93765873128`;
- exact source `8c7333a033cc8dbaf6676b1f9211d2906154277b`;
- exact libsignal submodule `857c4dca03537dc5e395a5e1eda6bf18f59c3601`;
- exact image `sha256:6a73d1eb2d4cf274540aa08e4e49e9e2e59bea51decafb7e313b545a6b5afa35`;
- state running / exit 0 / SQLite DB present / `PINNED_IMAGE_DB_STARTUP_GREEN`;
- artifact `9100012306`;
- digest `sha256:295ea802f18ba7e04327db01dd75f0be4c3d4df7db19d002831c49ada461d2af`;
- independently verified; exact one seven-field report only.

Do not rebuild these three merely to repeat evidence unless the implementation later changes their behavior.

## Facebook + LINE — ROOT CAUSE NOW PROVEN, OLD WARNINGS NONCAUSAL

The old Windows lines were misleading tails:

- Facebook `network.mode` null = upstream upgrader warning, not fatal; exact Meta validation allows empty mode.
- LINE `appservice.bot.avatar` null = upstream upgrader warning from empty upstream icon, not fatal.

A separate exact pinned-binary diagnostic was created so the user would not have to rerun Windows collector.

Workflow:

- `.github/workflows/multibridge-lab-fb-line-fatal-diagnostic.yml`
- verification-only commit `2991d16333ff274a141549ab4de2d4434f9cec10`
- run `31488170951`.

The diagnostic intentionally replays historical R12 non-DB wiring and intentionally does not apply recovered DB wiring.

### Facebook Personal — DATABASE_URI_NOT_CONFIGURED PROVEN

- job `93768243269`;
- exact Meta source `a0db68a56bb5715d67faa331f647e771d62b05a2`;
- exact default upstream Dockerfile;
- exact image `sha256:5130c03afcaf5de71a38c665dc533dc98622d37e843b9f879cfcb76339e3c06e`;
- state exited / exit code 11;
- classification `DATABASE_URI_NOT_CONFIGURED`;
- artifact `9099993096`;
- digest `sha256:a72e030dbc3ce884add6f4c053301a1dcc86f93f70604f25bd7f5e1f880f8aaa`;
- independently verified as one six-field report only.

### LINE — DATABASE_URI_NOT_CONFIGURED PROVEN

- job `93768243356`;
- exact LINE source `0fc10ea165b54db6ffd7c085d42cc42b0ce46414`;
- exact upstream LINE image;
- exact image `sha256:2718b804cd708df8a73f6bf1ba4d76b839291f8edc7f23cc836f9c8be0a9c933`;
- state exited / exit code 11;
- classification `DATABASE_URI_NOT_CONFIGURED`;
- artifact `9100068573`;
- digest `sha256:08d5653cbfafe2bc6feb86c4df563a83a77c9a83ef4c10c1ae2f4bc2e99446ed`;
- independently downloaded: ZIP digest exact match, exact file set only `pinned-fatal-line.txt`, six fields only, no logs/config/registration/token/DB.

Conclusion now frozen: **all five R12 services share the same historical database omission**.

## Exact next work package — START HERE IN NEW CHAT

Do not jump directly to implementation. The next causal step is a new failure-first scope expansion.

### Task A — test-only expansion RED

Update/add tests only first. Freeze expected database target set to exactly:

1. `facebook-personal`
2. `instagram-dm`
3. `google-messages`
4. `signal`
5. `line`

Non-targets must include at minimum:

- `telegram`
- `whatsapp`

Test expectations for Facebook and LINE:

- `Type = sqlite3-fk-wal`
- `Uri = file:/data/facebook-personal.db?_txlock=immediate`
- `Uri = file:/data/line.db?_txlock=immediate`
- same exact upstream-native yq DB fragment.

Current implementation must remain unchanged during this test-only commit. Expected causal RED: existing `Get-LabR12DatabaseWiring` returns `$null` for Facebook and LINE while all previous tests remain GREEN.

Record exact Windows RED before implementation.

### Task B — minimal five-target implementation

Only after causal RED:

- minimally extend the switch/mapping inside `tools/multibridge-lab/r12-database-wiring.ps1` to Facebook + LINE;
- no new framework, database daemon, migration layer, connection pool, YAML writer, Docker mutation engine, or second config system;
- preserve exact values already proven for the original three.

Then require full Windows GREEN.

### Task C — source-semantic authority expansion

Expand the immutable authority verification to Facebook and LINE.

Known source basis:

- Facebook shares Meta exact bridgev2 dependency with Instagram, so re-use only after fresh exact-source verification.
- LINE exact source `0fc10ea...` uses mautrix/go v0.28.0 exact commit previously verified as `a616b2b236fcb762e065ab1836b707aa71db3f46`, whose base validator contains the same placeholder URI → `database.uri not configured` predicate.

Fresh-fetch exact blobs before committing authority-fixture expansion.

Then require source-semantic Windows GREEN for all five.

### Task D — five-service exact pinned-image GREEN

Extend/reuse the existing isolated pinned-image validation for Facebook + LINE using the same mature upstream exact Docker/binary authorities already proven by the fatal diagnostic.

Apply the five-target repaired DB values plus exact historical non-DB R12 wiring. Require for Facebook and LINE:

- no `database.uri not configured`;
- no `Failed to initialize database`;
- no `Configuration error`;
- expected SQLite file present;
- process running / exit 0 after bounded observation.

Seal both artifacts independently. Do not repeat the already-sealed IG/GMessages/Signal build unless the shared implementation change creates a reason to rerun all five; if workflow matrix naturally runs all five, record results but do not treat repeats as new causal work.

### Task E — user-runtime repair/readiness package

Only after all five exact pinned binaries are config GREEN:

Build a failure-first Windows repair/readiness package rather than giving the user ad-hoc manual commands.

It must repair only the exact R12 database fields on the five bridge configs using the already-proven mapping, then validate progressively:

1. upstream config validation GREEN;
2. five processes sustained running;
3. RestartCount stable;
4. Compose endpoint/alias present;
5. Synapse → bridge DNS/TCP GREEN;
6. bridge → Synapse GREEN;
7. upstream provisioning/login surface GREEN;
8. only then restore `LAB_RUNTIME_READY`;
9. only then enter human account authorization in order:
   Facebook Personal → Instagram DM → Google Messages → Signal → LINE → Facebook Page last.

No credentials/config/tokens/logs containing user data may be uploaded to GitHub.

## Files to read first in new chat

1. `project-state/multibridge-lab/STATUS.md`
2. this handoff file
3. `docs/superpowers/plans/2026-08-11-yance-multibridge-lab-recovery.md`
4. `tools/multibridge-lab/r12-database-wiring.ps1`
5. `tests/multibridge-lab/r12-database-wiring.test.js`
6. `tests/multibridge-lab/r12-upstream-database-validator.test.js`
7. `tests/multibridge-lab/fixtures/r12-wire-bridge-config-expression.txt`
8. `tests/multibridge-lab/fixtures/upstream-database-validator-authorities.json`
9. `.github/workflows/multibridge-lab-pinned-db-image-validation.yml`
10. `.github/workflows/multibridge-lab-fb-line-fatal-diagnostic.yml`

## New-chat starter prompt

Use this exact intent:

`继续 YANCE-MULTIBRIDGE-LAB。先重新核验实时 GitHub，然后读取 project-state/multibridge-lab/STATUS.md 和 project-state/multibridge-lab/HANDOFF_2026-08-11_1908.md。不要重复 WhatsApp、Telegram、fatal-context collector、Instagram/GMessages/Signal pinned-image GREEN，也不要重新排查 Facebook/LINE warnings。当前五服务共享 R12 database omission 已经由 exact pinned binary 证实；现有 r12-database-wiring.ps1 仍只覆盖 IG/GMessages/Signal。立即从“五目标 DB mapping 的 failure-first test-only scope expansion”继续：先制造仅 Facebook/LINE 缺失导致的 causal RED，再最小扩展到五目标，随后做五目标 source-semantic + exact pinned-image GREEN，最后才构建用户 Windows runtime repair/readiness package。沿用完整授权：不逐项请示、禁止临时绕过、成熟 OSS 优先、不 force push/rebase/amend/squash、不弱化 gates，只在真实 RED、硬人工授权边界或最终 integration merge 边界停。`
