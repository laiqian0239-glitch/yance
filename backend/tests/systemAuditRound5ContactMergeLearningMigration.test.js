'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
test('identity merge keeps immutable learning signals under the existing platform-core repository authority',()=>{const root=path.resolve(__dirname,'..','..');const source=fs.readFileSync(path.join(root,'backend/repositories/platformCoreRepository.js'),'utf8');assert.match(source,/learning_signal_ledger/u);assert.match(source,/moveRelationshipLearningProfiles/u);assert.doesNotMatch(fs.readFileSync(path.join(root,'backend/services/contextAwareReplyBrain.js'),'utf8'),/replyLearningScopeAuthority/u);});
