'use strict';
const test=require('node:test');const assert=require('node:assert/strict');
const {createLearningEvaluationAdapter}=require('../services/learningEvaluationAdapter');const {createLearningPromotionAdapter}=require('../services/learningPromotionAdapter');
test('unreviewed or insufficient evidence cannot enter promotion',async()=>{const evaluation=createLearningEvaluationAdapter({promptfoo:null});const result=await evaluation.evaluate({Evidence:[]});assert.notEqual(result.status,'READY_FOR_REVIEW');});
test('promotion requires explicit human approval plus regression and shadow evidence',async()=>{const adapter=createLearningPromotionAdapter({openFeature:{setEvaluationContext(){}},flagd:{mode:'in-process-offline'}});const proposal={status:'READY_FOR_REVIEW',Regression:{passed:true},Shadow:{passed:true},Candidate:{id:'c'}};await assert.rejects(()=>adapter.promote(proposal,{approved:false}),e=>e.reasonCode==='LEARNING_APPROVAL_REQUIRED');const rollout=await adapter.promote(proposal,{approved:true});assert.equal(rollout.automaticPromotion,false);});
