'use strict';

// Required Product WP0 mount for the canonical active-conversation boundary.
// The detailed contracts remain in tests/p0 so legacy focused runners keep
// their existing paths, while test:wp0 executes them as release gates.
require('../p0/active-contact-store.test.js');
require('../p0/active-contact-integration-boundary.test.js');
