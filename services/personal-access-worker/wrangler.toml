name = "yance-personal-access"
main = "src/index.mjs"
compatibility_date = "2026-08-21"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

# UNKEY_ROOT_KEY must be supplied with `wrangler secret put UNKEY_ROOT_KEY`.
# Never place the root key in this file or expose it to Yance clients.
