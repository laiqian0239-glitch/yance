# 言策 f25fe2e 真实 Windows UAT 修复进展｜Batch 19

## 1. 本批结论

Batch 18 对用户大量截图中的问题理解错误：它把“整个生产平台采用固定视口嵌入式工作台，主要内容只能看到一部分”误判为若干首屏高度、标题遮挡和局部滚动问题，因此只压缩了头部、修改了 `min-height`，并保留了主要模块的固定网格与内部滚动。

Batch 19 不再做单页尺寸补丁，而是新增全平台公共布局权威，把九个生产工作区统一改为平面文档流：主要模块由内容自然撑高，当前页面使用一个主滚动根；桌面端允许有用的左右分栏，但分栏内容不再被限制在小窗口内。

真实 Windows Electron 尚未复测，因此本批不宣布问题关闭。

## 2. 源码身份

```text
IdentityMode=SHALLOW_REPAIR_SNAPSHOT
Branch=development/windows-uat-f25fe2e-repair-batch19-flat-document-flow
ImplementationCommit=3bfeec25197155af8f42eb281f8f8649360c71ed
ImplementationTree=613f6eed08c4688d9ac48027080d680aa1c96c8c
LogicalParentCommit=bbbead80910e0a8110f307d8983643947e5f9485
LogicalParentTree=6f299c4c2199d7a723fbfae8fa6f0fc22665e541
```

权威原始基线继续保持：

```text
Commit=f25fe2e2b4f065d2c09de034eddb67857eeb83bb
Tree=015f7969a2363952071bf11f4da3eb2adaf7edbf
```

本源码来自 Batch 18 载荷建立的浅层修复快照，交接材料不包含 Batch 18 Git 对象，因此 `LogicalParent` 表示逻辑继承关系，不声称已证明 Git 对象祖先关系。

## 3. 根因确认

此前多个后加载 CSS 权威互相冲突：

1. `html/body` 与应用壳采用固定桌面视口；
2. 生产工作区被强制 `height:100%`、`max-height:100%` 和 `overflow:hidden`；
3. 目录、详情、正文继续使用 `minmax(0,1fr)` 与 `overflow:auto`；
4. Batch 16/18 为了把更多内容挤入首屏，又加入固定最大高度、隐藏说明和压缩模块；
5. 自动检查只验证内容可通过内部滚动访问，没有验证主要模块是否处于同一个平面文档流。

这导致联系人、客户档案、关系轨迹、关系洞察、AI 工作台、账号与平台、系统中心、设置与恢复和主题页面都表现为“页面底部嵌入多个小窗口，每个窗口只显示部分内容”。

## 4. 实际修复

新增：

`frontend/r32-flat-document-flow.css`

并在 `frontend/index.html` 中作为最后一个布局权威加载。

修复内容：

- 九个生产页面由应用壳提供唯一主纵向滚动；
- 工作区根节点取消固定高度、最大高度和裁切；
- 联系人身份链、目录、详情、操作证据按自然高度参与文档流；
- 客户档案、关系轨迹、关系洞察目录与详情在桌面端保留左右分栏，但取消各自内部滚动；
- AI 工作台的核心状态、五个主标签、最近操作、撤销区和当前面板连续展开；
- 账号目录与账号工作台取消固定小视口；
- 系统中心、设置与恢复的导航和正文取消内部页面滚动；
- 主题工作室正文取消固定高度；
- 小窗口改为纵向自然展开；
- 非当前路由工作区保持隐藏，避免多个页面同时显示。

## 5. 回归与证据

```text
Batch 19 直接布局契约：5/5 PASS
Chromium DOM 几何矩阵：81/81 PASS
  9 个生产页面
  × 1512×931、1366×768、1920×1080
  × 100%、125%、150% 缩放
UAT 全套：131/131 PASS
源码 UAT 交付契约：33/33 PASS
主题审计：PASS
固定颜色债务：0
```

Chromium 几何矩阵逐项验证：

- 应用壳是主滚动权威；
- 当前工作区无固定最大高度；
- 主要模块不存在内部纵向滚动；
- 主要内容不存在裁切；
- 页面确实产生连续主滚动；
- 目录和详情保持有效文档顺序。

第一次矩阵运行发现公共选择器错误地让非当前工作区也显示；该副作用已修复并重新执行，最终 81/81 通过。

本轮尝试执行 `npm ci` 时工具调用异常，并产生不完整依赖树；该目录已删除，不把本次尝试记为依赖安装通过，也不声明全后端全集通过。

## 6. 仍需真实 Windows 验证

必须在真实 Windows Electron 中逐页证明：

- 页面最右侧只有一根主要纵向滚动条；
- 身份链、联系人详情、操作记录等主要模块不再嵌入固定高度小窗口；
- 左右分栏存在时，两栏均由内容自然撑高，不存在独立纵向滚动；
- 页面可以从顶部连续滚动到最后一个模块；
- 100%、125%、150% Windows 缩放都不重新出现裁切；
- 动态加载真实联系人、账号、诊断、学习和主题数据后仍满足；
- 会话消息历史、弹窗和明确技术日志等允许的独立滚动不受破坏。

AI 回复闭环、真实 OpenRouter、真实三平台发送与成功发送后学习等原有阻断项仍需继续真实验收。

## 7. 治理状态

```text
REPAIR_ATTEMPT_IN_PROGRESS
WINDOWS_UAT_BLOCKED
formalRelease=false
readyForPromotion=false
```
