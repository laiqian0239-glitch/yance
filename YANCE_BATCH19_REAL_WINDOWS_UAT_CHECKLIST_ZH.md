# Batch 19 真实 Windows 平面布局验收清单

## 启动前

1. 从系统托盘完全退出旧言策。
2. 不删除现有账号，不重新扫码，不删除真实业务数据。
3. 把 Batch 19 的 CMD、PowerShell 和完整源码 ZIP 放在电脑“下载”目录。
4. 双击 `START_YANCE_F25FE2E_BATCH19_WINDOWS_UAT.cmd`。
5. 启动日志必须显示 Batch 19 源码哈希和隔离数据目录。

## 核心判断方法

打开任意生产页面后，先观察滚动条：

- 页面右侧应只有一根主要纵向滚动条；
- 身份链、联系人列表、联系人详情、操作记录、关系图表、诊断、设置正文等主要模块内部不应再出现自己的纵向滚动条；
- 鼠标位于任意主要内容区滚动时，应移动整个页面；
- 允许左右分栏，但左右栏必须完整展开，不得成为两个只能看到一部分内容的小窗口。

“内部滚动以后可以看到”不算通过。

## 逐页检查

### 1. 联系人与身份

- 顶部状态、统计、身份与关系链完整显示；
- 联系人目录与当前联系人详情处于同一平面；
- 当前联系人详情、身份字段、操作记录和后续内容完整展开；
- 不再把联系人列表和详情塞入底部固定高度工作台。

### 2. 客户档案

- 客户目录与档案详情自然展开；
- 事实、证据、长期备注、学习记录和待确认内容均由页面主滚动访问；
- 详情标题下方内容不被固定容器裁切。

### 3. 关系轨迹

- 联系人目录与轨迹详情完整展开；
- 关系温度图表、互动节奏诊断、关键话题影响和后续模块连续排列；
- 图表下面的模块不是只露标题或一小部分。

### 4. 关系洞察

- 关系对象目录和洞察详情完整展开；
- 指标、机会、风险、开放事项和下一步动作连续浏览；
- 不存在详情小窗口内部滚动。

### 5. AI 工作台

- 智能核心、五个主标签、最近操作、撤销入口和当前配置面板均可见；
- 导演规则、学习治理、模型服务、任务路由、人物基线的内容自然撑开；
- 不再把当前面板嵌入固定高度窗口。

### 6. 账号与平台

- 账号目录和当前账号详情在同一页面平面；
- 能力、绑定、认证、发送路由、诊断和操作历史完整展开；
- 已连接账号不得因布局切换丢失或要求重新认证。

### 7. 系统中心

- 所有系统入口完整可达；
- 当前系统分区的状态、诊断和日志摘要连续展开；
- 主要系统正文不再是内部滚动页面。

### 8. 设置与恢复

- 所有设置入口完整可达；
- 设置卡、备份、恢复、迁移及说明连续展开；
- 按钮不得覆盖说明文字。

### 9. 主题与外观

- 主题列表、预览、个性化、字体、动效和可访问性模块连续展开；
- 主题正文不被固定高度容器截断；
- 29 套主题切换后布局结构不改变。

## 缩放矩阵

以上九页分别在 Windows 显示缩放或应用缩放下检查：

```text
100%
125%
150%
```

任何一个比例重新出现固定高度小窗口、主要模块内部滚动或内容只显示一部分，都判定失败。

## 允许的内部滚动例外

以下区域可以保留内部滚动：

- 会话消息历史；
- 弹窗和下拉菜单；
- 明确标识的代码或技术日志查看器；
- 经确认的超长虚拟化数据表。

## 通过标准

只有九个生产页面在真实 Windows Electron、真实数据和三档缩放下全部满足平面展开与单一主滚动，才允许关闭本项全平台布局阻断。
