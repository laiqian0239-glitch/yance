name: OSS-1A Task 6 Candidate Builder

on:
  push:
    branches:
      - work/oss1a-task6-generator

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm tooling workflow registration
        run: echo OSS1A_TASK6_TOOLING_WORKFLOW_REGISTERED
